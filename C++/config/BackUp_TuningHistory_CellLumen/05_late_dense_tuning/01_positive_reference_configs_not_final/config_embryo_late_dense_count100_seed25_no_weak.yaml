# Late dense CellLumen tuning candidate.
# Increases the seed-center pull. This is a control to see whether the local
# maximum seed is closer to the GT center for the remaining hard late cells.
base_config: config_embryo_late_dense_minvox1000_count100.yaml

cell_lumen:
  seededWatershedCenterSeedBlend: 0.25
  finalWeakSatelliteFilterEnabled: false

