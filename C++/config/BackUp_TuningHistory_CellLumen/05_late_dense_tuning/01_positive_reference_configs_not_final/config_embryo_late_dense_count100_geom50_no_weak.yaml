# Late dense CellLumen tuning candidate.
# Uses a stronger geometric center blend so late dim cells are less pulled
# toward uneven bright subregions. The detector remains the same; only center
# placement changes.
base_config: ../01_positive_reference_configs_not_final/config_embryo_late_dense_minvox1000_count100.yaml

cell_lumen:
  geometricCenterBlend: 0.50
  finalWeakSatelliteFilterEnabled: false

