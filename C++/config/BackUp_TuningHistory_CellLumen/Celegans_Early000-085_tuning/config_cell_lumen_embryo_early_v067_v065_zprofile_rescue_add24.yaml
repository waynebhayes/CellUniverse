base_config: config_cell_lumen_embryo_early_v065_v061_zprofile_rescue_add4.yaml

cell_lumen:
  # Diagnostic only: raise the z-profile rescue budget high enough to verify
  # whether the f036 upper z candidate exists but is ranked too low.
  finalZProfileRescueMaxAdded: 24
